-- Add profile_picture column to users table if it doesn't exist
ALTER TABLE users 
ADD COLUMN IF NOT EXISTS profile_picture VARCHAR(255) DEFAULT NULL 
COMMENT 'مسار صورة الملف الشخصي للمستخدم';

-- Update existing users to have a default profile picture path if needed
-- UPDATE users SET profile_picture = NULL WHERE profile_picture = '';
